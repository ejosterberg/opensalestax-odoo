<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: LGPL-3.0-or-later OR AGPL-3.0-or-later -->
<odoo noupdate="1">

    <!-- v0.2.1 — Bulk OST recompute server action.
         Exposed on the account.move list view under the Action
         menu. Operates on the selected recordset; idempotent;
         skips moves that aren't draft or don't engage OST.

         Useful after a rate-table change on the engine side:
         filter Bills → All → state=Draft and run this to refresh
         every draft's tax breakdown without opening each one. -->
    <record id="action_server_ostax_bulk_recompute_drafts"
            model="ir.actions.server">
        <field name="name">OpenSalesTax: bulk recompute drafts</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_ostax_bulk_recompute_drafts()</field>
    </record>

</odoo>
